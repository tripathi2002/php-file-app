# UI v1 (frozen)

This folder is the **v1 UI snapshot**. It must keep calling **API v1** (`/api/v1/` and the same merge body as `/merge.php`).

Do not point these files at `/api/v2/` or at the latest root `modules/`. Compatible bugfixes only.

See [docs/versioning.md](../../docs/versioning.md).
