# UI v2 (active)

Development UI for **API v2** (`/api/v2/`). Same baseline as v1 today; v2 requirements get implemented here and in `api/v2/`.

- Entry: `/ui/v2/` or root `/` (latest)
- API: `/api/v2/`
- Freeze to this folder when v3 ships

See [docs/api-v2.md](../../docs/api-v2.md) and [docs/versioning.md](../../docs/versioning.md).
